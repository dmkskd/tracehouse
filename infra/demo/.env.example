# TraceHouse Demo Environment
# Generate with: ./setup.sh
# Or copy and fill in manually: cp .env.example .env

# ─── ClickHouse default user (admin, restricted to localhost/Docker) ───
CLICKHOUSE_DEFAULT_PASSWORD=

# ─── ClickHouse read-only user (externally accessible via HTTPS) ───
CLICKHOUSE_READONLY_PASSWORD=
